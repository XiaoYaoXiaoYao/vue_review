<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.liandi.todoback.dao.UserDao">
    <select id="getUserByMassage" resultType="com.liandi.todoback.bean.User">
    select * from tengyt_user where username=#{username} AND password=#{password} AND ustate = '1'
    </select>
</mapper>